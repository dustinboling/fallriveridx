require "spec_helper"

describe AgentMailer do
  it "emails an agent with the correct information" do
    agent = Factory(:agent)
  end

  it "emails agents passed in from the agent mailer rake task" do
  end

  it "logs the time and date that the email was sent" do
  end

  it "logs whether the email was successful" do
  end
end
